#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int x = 10;
    int y = 5;

    cout << min(x, y) << endl;
    cout << max (x, y) << endl;
    cout << sqrt(25);

    return 0;
}